services:
  reservations:
    build:
      context: ./reservations
      dockerfile: ./dockerfile
    entrypoint: python -m pytest
  
  restaurant:
    build:
      context: ./restaurant
      dockerfile: ./dockerfile
    ports:
      - 8000:8000
    env_file:
      - ./restaurant/.env.docker
    entrypoint: python -m pytest